# Changelog

## Unreleased

- Rebrand to JoshTapApp; consolidated NFC architecture into `NfcViewModel`.
- Room DB migration v1 → v2 (added `nfcToken`).
- Removed tracked build artifacts and tightened `.gitignore`.
- Docs reorganized under `docs/`.
